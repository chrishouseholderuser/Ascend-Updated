/*
  Public browser configuration. Copy config.example.js to this file during
  deployment and supply the values from Supabase → Project Settings → API.
  The anon/publishable key is safe to expose; never put service_role keys here.
*/
window.ASCEND_CONFIG = window.ASCEND_CONFIG || {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
